{
  "dependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0",
    "sass": "^1.77.5"
  },
  "scripts": {
    "postwatch": "npx postcss src/styles.css --use autoprefixer -d build/ --watch",
    "autoprefixer": "npx postcss src/styles.css --use autoprefixer -d build/",
    "sassw": "npx sass --watch src/sass/styles.scss src/styles.css"
  }
}
